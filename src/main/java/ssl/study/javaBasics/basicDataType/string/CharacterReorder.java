package com.supconit.study.JavaBasics.basicDataType.string;

/**
 * 给定一个只包含是、大小写英文的字符串，需要将字符串中出现的字符按照一定的规则重新排序后输出，排序规则如下：
 * 1. 首先按照字符串里字符出现的频率降序排列；
 * 2. 对于频率相同的字符，则按照先大写后小写的顺序排列；
 * 3. 对于频率相同且大小写相同的字符，则按照字母的顺序排列，即a/A排在最前，z/Z排在最后，中间的字母以此类推。
 * 示例；
 * 输入baAbb
 * 输出bbbAa
 */
public class CharacterReorder {
}
